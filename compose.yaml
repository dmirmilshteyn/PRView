services:
  prview:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
    working_dir: /workspaces/PRView
    command: sh -lc "bun install --frozen-lockfile && bun run dev"
    ports:
      - "9999:9999"
    volumes:
      - .:/workspaces/PRView
      - /workspaces/PRView/node_modules
